const progressBarStyles = {
  wrapper: "block",
  label: "mb-[var(--space-2)] flex justify-between text-xs text-[var(--text-muted)]",
  root: "h-2 overflow-hidden rounded-full bg-[var(--surface-muted)]",
  bar: "h-full rounded-full bg-[var(--accent)]",
};

export { progressBarStyles };
